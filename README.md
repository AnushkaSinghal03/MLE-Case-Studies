Case Study 1 (Hospital Readmission): Used Logistic Regression with L2 Regularization. Dropped ID columns, encoded categorical variables, and evaluated with ROC-AUC.

Case Study 2 (Credit Card Fraud): Used XGBoost and handled class imbalance using SMOTE on the training data. Lowered decision threshold to catch more fraud.
